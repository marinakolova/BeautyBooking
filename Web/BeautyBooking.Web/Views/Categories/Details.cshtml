@model BeautyBooking.Web.ViewModels.Categories.CategoryViewModel

@{
    this.ViewData["Title"] = Model.Name;
    var serviceNumber = 0;
}

<div class="row">

    <!-- News Posts -->
    <div class="col-lg-8">
        <div class="news_posts">

            <!-- News Post -->
            <div class="news_post">
                <div class="news_post_image"><img src="@Model.ImageUrl" alt=""></div>
                <div class="news_post_content">
                    <div class="news_post_title"><a>@Model.Name</a></div>
                    <div class="news_post_text mb-4">
                        <p>@Model.Description</p>
                    </div>

                    <h3 class="mb-3">Beauty Services in this category:</h3>

                    @foreach (var service in Model.Services)
                    {
                        serviceNumber++;
                        <div class="accordion_container">
                            @if (serviceNumber == 1)
                            {
                                <div class="accordion d-flex flex-row align-items-center active"><div>@service.Name</div></div>
                            }
                            else
                            {
                                <div class="accordion d-flex flex-row align-items-center"><div>@service.Name</div></div>
                            }
                            <div class="accordion_panel">
                                <div>
                                    <p>@service.Description</p>
                                </div>
                            </div>
                        </div>
                    }

                </div>
            </div>

        </div>
    </div>
</div>